//console.log("You made it to the LandscapeProtrait.js!")

let stop = false;

function testNaN (number) {
  console.log(stop);
  if ( isNaN (number) {
    stop = true;
    return "Type a Real Number"
  } else {
    return "Input validated"
  }
}

function geometry (width, height) {

// This coding illustrated the advanced method
function main (// get the value of the width input field
firstNumber = document.getElementById("textfield1").value;


//valuidate if user typed a number, odd validation by TRUE conditional
document.getElementbyId("validiytest1").innerHTML = alert( testNaN (firstNumber);
document.getElementbyId("validiytest1").innerHTML = testNaN (firstNumber);


// Get the value of the Height Input Field, Id="textfield2"
secondNumber = document.getElementById("textfield2").value;

//valuidate if user typed a number, odd validation by TRUE conditional
document.getElementbyId("validiytest2").innerHTML = alert( testNaN (secondNumber);
document.getElementbyId("validiytest2").innerHTML = testNaN (secondNumber);

if (stop == true) {
  document.getElementById("large").innerHTML = "restart because I said so."
}
else {
  console.log("what did you say."), geometry (firstNumber, secondNumber));
  document.getElementById("large").innerHTML = "what did you say" + geometry (firstNumber, secondNumber);
 }
}
) {

     let (testNumber,)
