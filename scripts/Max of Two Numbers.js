console.log("You made it to the childtemplate.js!")

function main () {=

  let firstNumber, secondNumber, text;

// get the value of the width input field
firstNumber = document.getElementById("textfield").value;


//valuidate if user typed a number, odd validation by TRUE conditional
document.getElementbyId("validiytest").innerHTML = alert( testNaN (firstNumber);
document.getElementbyId("validiytest").innerHTML = testNaN (firstNumber);


// Get the value of the Height Input Field, Id="textfield2"
secondNumber = document.getElementById("textfield2").value;

//valuidate if user typed a number, odd validation by TRUE conditional
document.getElementbyId("validiytest").innerHTML = alert( testNaN (secondNumber);
document.getElementbyId("validiytest").innerHTML = testNaN (secondNumber);

if (stop == true) {
  document.getElementById("large").innerHTML = "restart because I said so."
}
else {
  console.log("what did you say."), geometry (firstNumber, secondNumber));
  document.getElementById("large").innerHTML = "what did you say" + geometry (firstNumber, secondNumber);

 }
}
